# Copy to .env (git-ignored) and start with: node --env-file=.env src/app.js
# Every variable has a sensible default; none is required.

# TCP port the API listens on (default: 3000)
PORT=3000

# Reported by GET /health; the Docker image sets "production" (default: development)
NODE_ENV=development
